# Напишите программу, которая принимает на вход цифру, обозначающую день недели, и проверяет, является ли этот день выходным.

N = int (input ("Введите номер дня недели: "))
if N > 5 and N <= 7:
    print ("Выходной")
elif N < 5 and N >= 1:
    print ("Рабочий")
else:
    print ("Номер дня введен неверно")

# Напишите программу, которая принимает на вход координаты точки (X и Y), причём X ≠ 0 и Y ≠ 0 и 
# выдаёт номер четверти плоскости, в которой находится эта точка (или на какой оси она находится).

x = float(input("Введите значение Х: "))
y = float(input("Введите значение Y: "))
if x > 0 and y > 0:
    print ("Точка находится в первой плоскости")
elif x < 0 and y > 0:
        print ("Точка находится во второй плоскости")
elif x < 0 and y < 0:
    print ("Точка находится в третьей плоскости")
elif x > 0 and y < 0:
    print ("Точка находится в четвертой плоскости")
else:
    print ("Х и (или) Y не может равняться нулю")

# Напишите программу, которая по заданному номеру четверти, показывает
# диапазон возможных координат точек в этой четверти (x и y).

N = int(input("Введите номер плоскости от 1 до 4: "))
if N == 1:
    print("Х от 0 до +∞, Y от 0 до +∞")
elif N == 2:
    print("Х от 0 до -∞, Y от 0 до +∞")
elif N == 3:
    print("Х от 0 до -∞, Y от 0 до -∞")
elif N == 4:
    print("Х от 0 до +∞, Y от 0 до -∞")
else:
    print("Номер плоскости указан неверно")

# Напишите программу, которая принимает на вход координаты двух точек
# и находит расстояние между ними в 2D пространстве.
import math
x1 = float(input("Введите значение Х первой точки: "))
y1 = float(input("Введите значение Y первой точки: "))
x2 = float(input("Введите значение Х второй точки: "))
y2 = float(input("Введите значение Y второй точки: "))
L1 = x1 - x2
L2 = y1 - y2
L3 = math.sqrt((L1**2) + (L2**2))
print(f"Расстояние между точками равно {L3}")

# Напишите программу для. проверки истинности утверждения
# ¬(X ⋁ Y ⋁ Z) = ¬X ⋀ ¬Y ⋀ ¬Z для всех значений предикат.

def reliability(x, y, z):
    print(f"¬({x} ⋁ {y} ⋁ {z}) = ¬{x} ⋀ ¬{y} ⋀ ¬{z} is {(not (x or y or z)) == (not x and not y and not z)}")
    return (not (x or y or z)) == (not x and not y and not z)
if (reliability(0, 0, 0) and reliability(0, 0, 1) and reliability(0, 1, 0) and 
reliability(0, 1, 1) and reliability(1, 0, 0) and reliability(1, 0, 1) and
reliability(1, 1, 0) and reliability(1, 1, 1)):
    print("Истинно")
else:
    print("Ложно")
